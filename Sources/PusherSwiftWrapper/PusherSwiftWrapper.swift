public struct PusherSwiftWrapper {
    // This is required because of limited SPM support for binary products with dependencies
    /*
    public private(set) var text = "Hello, World!"

    public init() {
    }
     */
}
